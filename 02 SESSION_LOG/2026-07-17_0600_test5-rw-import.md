---
session: 2026-07-17_0600_test5-rw-import
agent: Claude (Sonnet 4.6)
---

# Session: Test 5 RW Import + T4 Rationale/Chart Patch

## COMPLETED

**Step 1 — T4-RW rationales patched (66/66, 0 errors)**
- Re-parsed `test4_explanations.txt` (UTF-8 with Unicode curly quotes)
- `scripts/patch-rw-rationales.ts` batch-updated all 66 T4-RW questions
- Fix required: replaced `import dotenv` with manual `.env.local` parser (no dotenv package installed)

**Step 2 — T4-RW chart images uploaded**
- T4-RW-M1-Q13: extracted via `page.get_drawings()` (vector bar chart, left column), uploaded PNG to `question-assets/charts/`
- T4-RW-M2-Q13: same approach
- `media_urls` JSONB field updated in DB for both rows via Supabase REST API PATCH

**Step 3 — Test 5 RW: 66 questions extracted and imported**
- `parse_test5_questions.py` (v4): sequential-number validation algorithm (not dotted-separator split) → 33 M1 + 33 M2
- Key fixes: M2-Q1 leading dash (`- 1`), module-number false-positive filter (`prev == 'Module'`), axis-value skip (`prev AND next_l are short digit strings`)
- Answer keys from `test5_scoring.txt`; rationales from `test5_explanations.txt` (33/33 each module)
- Chart detection: added `'uses data from the graph'` flat-text check (newline-normalized) → detected M1-Q16 and M2-Q12
- Table questions (M1-Q14, M2-Q16) correctly excluded from chart flag (table text embedded in stem)
- Chart extraction: `extract_t5_charts.py` using `page.get_drawings()` + y>80pt header skip
  - M1-Q16: Manganese/Calcium ratio line graph (page 10, left column, 71KB)
  - M2-Q12: Total Science Research Submissions by Topic line chart (page 23, left column, 64KB)
- Both uploaded to Supabase Storage `question-assets/charts/`
- `scripts/test5_rw_questions.json` written (66 records), imported via `npx tsx scripts/import-official-bank.ts`
- Result: 66/66 imported, 0 skipped

**Step 4 — Audit**
- `npm run audit:question-bank` → 261 total, 0 SEVERE, 22 WARN (pre-existing PLACEHOLDER rows with null choices)

## DECISIONS

- **Chart detection fix**: `has_chart` via `'uses data from the graph'` (flat, newline-normalized) rather than axis-noise counting, because graph appears in the preceding question's PDF block (chart is in Q15's text column; Q16's text has no axis values)
- **Table vs graph**: `'uses data from the table'` → no image needed (table text embedded in stem). Only `'uses data from the graph'` → image upload required
- **Skill mapping**: reused T4 RW pattern for T5 (same standard digital SAT ordering). Not CollegeBoard-official but consistent with existing T4 data
- **`media_urls` placeholder not used**: final JSON sets the actual public Supabase Storage URL directly (rather than a placeholder), so no second-pass patch script needed

## SIGN-OFF

Claude (Sonnet 4.6) — 7/17/26 ~6:00 AM
